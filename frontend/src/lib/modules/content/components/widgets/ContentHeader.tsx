import React from 'react';
import { PageHeader } from '@/components/ui/PageHeader';

export const ContentHeader = () => {
    return (
        <PageHeader
            title="Content Management"
            subtitle="Customize your landing page content, visuals, and messaging."
        />
    );
};
